# rebuy-go-sdk full example

This is an example with all kinds of features from the SDK. It reflects the
current best practices and should be used as a reference when adding those
features to an application.
